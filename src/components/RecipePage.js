import React from 'react';

const RecipePage = ({ title }) => {
  return (
    <div>
      <h1>{title}</h1>
      {/* Additional recipe page content */}
    </div>
  );
};

export default RecipePage;
